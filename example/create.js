var createQR = require('../index.js').createQR,
    http = require('http');

var options = {
    remote: "",
    username: "zenozeng",
    key: ""
}

http.createServer(function(req, res) {
    
}).listen(3001);